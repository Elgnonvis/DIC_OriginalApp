<div class="w-50 d-flex flex-column container justify-content-center align-itmes-center mt-3" style = "padding-top: 1rem; border: 1px solid;">

    <p><strong>Description:  </strong><%= @appointment.description %></p>

    <p><strong><%= t("properties.status") %>  : </strong><%= @appointment.status %></p>

    <p><strong>Date:  </strong><%= @appointment.date.strftime("%d %b %Y à %H:%M") %></p>

    <p><strong><%= t("appointments.requested_by") %> :   </strong><%= @appointment.user.username %></p>
    <a href = "<%= edit_appointment_path(@appointment) %>" class="btn btn-warning mb-2"><%= t("button.edit") %></a>

    <a href = "<%= appointments_path %>" class="btn btn-success mb-2"><%= t("button.back") %></a>
</div>
